{% extends 'base.html.twig' %}

{% block title %}MedicalRecord{% endblock %}

{% block body %}
    <h1>MedicalRecord</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medical_record.id }}</td>
            </tr>
            <tr>
                <th>Diagnosis</th>
                <td>{{ medical_record.diagnosis }}</td>
            </tr>
            <tr>
                <th>TreatmentPlan</th>
                <td>{{ medical_record.treatmentPlan }}</td>
            </tr>
            <tr>
                <th>CreatedDate</th>
                <td>{{ medical_record.createdDate ? medical_record.createdDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_medical_record_index') }}">back to list</a>

    <a href="{{ path('app_medical_record_edit', {'id': medical_record.id}) }}">edit</a>

    {{ include('medical_record/_delete_form.html.twig') }}
{% endblock %}
